// to print ascii codeof a character

#include <stdio.h>

int main()
{
    char character;

    printf("Input character: ");
    scanf("%c", &character);

    printf("ASCII code: %d", character);
}